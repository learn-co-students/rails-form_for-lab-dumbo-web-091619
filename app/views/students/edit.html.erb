<h1>Editing Student</h1>

<h2>Student Form</h2>
<%= form_for(@student) do |f| %>
<%= f.text_field :first_name %>
<%= f.text_field :last_name %>
<%= f.submit 'Edit!', id: 'Update Student' %>
<% end %>


<%= link_to 'Show', @student %> |
<%= link_to 'Back', students_path %>
