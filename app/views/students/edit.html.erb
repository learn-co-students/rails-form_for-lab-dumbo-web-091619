<%= content_tag(:h1, "Update Student") %>
<%= form_for(@student) do |student_form| %>
  <%= student_form.text_field :first_name %>
  <%= student_form.text_field :last_name %>
  <%= student_form.submit "Update Student" %>
<% end %>